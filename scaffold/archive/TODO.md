- [ ] **Website: add Lemon Squeezy checkout URL** — paste real checkout link into `website/checkout.json` (LS product → Share → checkout URL). Set Cloudflare Pages secrets `LEMONSQUEEZY_API_KEY` and optional `LEMONSQUEEZY_STORE_ID` for gated `/thanks.html`. Set LS post-purchase redirect to `https://YOUR_DOMAIN/thanks.html?order_id=[order_id]`.
- [x] **Hide dev tabs (Lesson Generator, Database, About/Help)** — tabs removed from main window; `LessonGenerator` kept off-tab for `auto_review`. — Composer, 2026-06-01
- [ ] **Weakspot: explain/clarify "damage"** in UI or docs — SQL uses `total*time²*(1+misses/total)` as impact score from Analysis; selection should feel like "slow + frequent + costly mistakes", not opaque. Optional: expose weakspot-specific selection knobs later.
- [ ] **Weakspot: target selection & scoring** — balance slowness vs frequency without always picking global worst; avoid weakspot feedback loop. Scoring is `time²·(count+misses)` (damage; frequency linear/decisive). Importance-weighted repetition (`allocate_repeats`) and cross-lesson freshness (`recent` de-emphasis from 2-lesson deque in `WeakSpot.py`) are in place. `<Weakspot>` stat exclusion done (Composer, 2026-06-01): `statistic.source` column + migration; typer/quizzer write source; fetch excludes discounted sources; `<Weakspot>` marked `discount=1`. STILL OPEN: per-lesson repeat caps and pool rotation across lessons.
- [x] **Remove legacy Typer** — removed `Quizzer.py`, `which_typer`, and `quiz_*` color settings; inline typer is the only implementation. — Composer, 2026-06-07

- [x] **Weakspot: hyper-compress targets** — pack as many weak chars/trigrams/words into each phrase as possible (goal: 5+ hits in one word or biword when feasible); score by `(targets_hit, total_chars)`; merge adjacent micro-phrases when a single longer join preserves trigram boundaries. — Claude Opus 4.8, 2026-06-01
- [x] **Weakspot: smarter trigram matching** — cross-word and punctuation trigrams (`ol,`, `," `, `-fe`, `At `, `o,"`, ` Al`, etc.); unified boundary rules with `_surface_end` / open-quote variants; dict only when no weak word fits. — Claude Opus 4.8, 2026-06-01
- [x] **Weakspot: coverage guarantee** — each composable target appears at least once per lesson; `covered_targets()` substring check drives the build loop. — Claude Opus 4.8, 2026-06-01
