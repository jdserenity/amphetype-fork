{
  "name": "typing-program-website",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "deploy": "wrangler pages deploy . --project-name=typing-program"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
