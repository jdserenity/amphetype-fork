- [ ] Remove legacy Typer 1.0 / Quizzer implementation (the `Quizzer` widget and old `Typer` class in `amphetype/Quizzer.py`, the `quiz_*` color settings, and related code paths) after the inline typer has been the default for a sufficient transition period and is considered stable. (Decision by owner, 2026-04; recorded by Grok)

- (No other items yet.)
- [x] **Weakspot: hyper-compress targets** — pack as many weak chars/trigrams/words into each phrase as possible (goal: 5+ hits in one word or biword when feasible). Approach: constraint-satisfaction / greedy search over weak-word pool + dict lookup tokens; score phrases by `(targets_hit, total_chars)` and prefer higher density; merge adjacent micro-phrases when a single longer join preserves all trigram boundaries. — done by Claude Opus 4.8, 2026-06-01 (hardest-first packing; slot fillers chosen to cover extra targets; already-covered targets skipped; ~14 tokens for 16 targets in practice)
- [x] **Weakspot: smarter trigram matching** — many cross-word and punctuation trigrams still missed (`ol,`, `," `, `-fe`, `At `, `o,"`, ` Al`, etc.). Approach: unify on boundary rule — if middle char is space, left token ends with `[0]` / right starts with `[2]` (with `_surface_end` / open-quote variants); if no space, substring in one token OR split across `-`, `'`, `"`; handle leading-space trigrams (` Al`) as start-of-lesson or prepend surface form; only use dict for token lookup when no weak word fits, never as lesson padding. — done by Claude Opus 4.8, 2026-06-01 (full space-pattern handling on substring ground truth; all listed shapes covered by tests)
- [ ] **Weakspot: target selection & scoring** — balance slowness vs frequency without always picking global worst; avoid weakspot feedback loop (practicing an item inflates its count → more selection). Approach: composite score e.g. `slowness * log1p(count)` with **down-weight or exclude stats where `source` = `<Weakspot>`** (or discount count from weakspot-only rows); cap how often same target appears per lesson; rotate pool so enough lessons cover all weak items over time rather than one lesson = only top-N damage. — PARTIAL (Claude Opus 4.8, 2026-06-01): scoring now `time²·log1p(count)·(1+misses/count)` so slowness dominates and frequency is log-dampened (structurally softens the feedback loop). STILL OPEN: hard exclusion of `<Weakspot>`-sourced stats needs a `source` column on `statistic` (schema migration + writes in `typer.py`/`Quizzer.py`); plus per-lesson repeat caps and pool rotation across lessons.
  - UPDATE (Claude Opus 4.8, 2026-06-01): owner reversed the scoring direction — importance must win, a fast item typed 100× should dominate a slow item typed once. Scoring is now `time²·(count+misses)` (damage; frequency linear/decisive). Added importance-weighted **repetition** (`allocate_repeats`: each target ≥1, repeats ∝ score, capped) so high-impact items get real practice; and **cross-lesson freshness** (fresh RNG per build → varied realizations/order, plus a `recent` de-emphasis set wired from a 2-lesson deque in `WeakSpot.py`). `<Weakspot>` stat exclusion done (Composer, 2026-06-01): `statistic.source` column + migration; typer/quizzer write source; fetch excludes discounted sources; `<Weakspot>` marked `discount=1` so stats not written by default.
- [ ] **Weakspot: explain/clarify "damage" in UI or docs** — today SQL uses `total*time²*(1+misses/total)` as impact score from Analysis; selection should feel like "slow + frequent + costly mistakes", not opaque. Optional: expose weakspot-specific selection knobs later.
- [x] **Weakspot: coverage guarantee** — ensure each selected target appears at least once per lesson when composable; track `covered` set against actual trigram/char presence in emitted text, not just batch membership. — done by Claude Opus 4.8, 2026-06-01 (`covered_targets()` substring check drives the build loop; lesson stops when all composable targets are present)
- [ ] Hide **Lesson Generator** tab once Weakspot is stable (keep code for dev). — owner request 2026-06-01
